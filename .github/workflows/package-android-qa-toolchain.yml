name: Package Android QA Toolchain

on:
  push:
    paths:
      - '.github/workflows/package-android-qa-toolchain.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  package:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - name: Install Android QA components
        shell: bash
        run: |
          set -euxo pipefail
          echo "ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT"
          SDKMANAGER="$ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager"
          if [ ! -x "$SDKMANAGER" ]; then
            SDKMANAGER=$(find "$ANDROID_SDK_ROOT/cmdline-tools" -type f -name sdkmanager -perm -111 | head -1)
          fi
          test -n "$SDKMANAGER"
          yes | "$SDKMANAGER" --sdk_root="$ANDROID_SDK_ROOT" --licenses >/dev/null || true
          "$SDKMANAGER" --sdk_root="$ANDROID_SDK_ROOT" \
            "platform-tools" \
            "emulator" \
            "cmdline-tools;latest" \
            "system-images;android-35;default;x86_64"
          test -x "$ANDROID_SDK_ROOT/platform-tools/adb"
          test -x "$ANDROID_SDK_ROOT/emulator/emulator"
          test -f "$ANDROID_SDK_ROOT/system-images/android-35/default/x86_64/package.xml"

      - name: Package runtime only
        shell: bash
        run: |
          set -euxo pipefail
          cd "$ANDROID_SDK_ROOT"
          tar -cf - \
            platform-tools \
            emulator \
            cmdline-tools/latest \
            system-images/android-35/default/x86_64 \
            | zstd -T0 -10 -o "$RUNNER_TEMP/android-qa-sdk-api35-x86_64.tar.zst"
          cd "$RUNNER_TEMP"
          sha256sum android-qa-sdk-api35-x86_64.tar.zst > SHA256SUMS.txt
          ls -lh android-qa-sdk-api35-x86_64.tar.zst SHA256SUMS.txt

      - name: Upload temporary bridge artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-qa-sdk-api35-x86_64
          path: |
            ${{ runner.temp }}/android-qa-sdk-api35-x86_64.tar.zst
            ${{ runner.temp }}/SHA256SUMS.txt
          compression-level: 0
          retention-days: 1
          if-no-files-found: error
